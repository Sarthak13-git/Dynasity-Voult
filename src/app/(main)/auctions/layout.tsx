export const metadata = {
  title: "Auctions | Dynasity-Voult",
  description: "Explore our curated selection of extraordinary auction items.",
};

export default function AuctionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
